  // Find pairs with given sum in doubly linked list
#include<iostream>
#include "doub_LL_Main.h"

   // create pair function 

int main()
{
	    node*head=NULL;
    insertAthead(head,1);
    insertAttail(head,2);
    insertAttail(head,4);
    insertAttail(head,5);
    insertAttail(head,6);
    insertAttail(head,8);
    insertAttail(head,9);





    display(head);		
}